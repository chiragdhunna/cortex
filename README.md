# cortex
Cortex is a multimodal notes-generation agent that turns PDFs, audio, video, and web links into structured notes. It decouples category-aware LLM generation from output formatting through a canonical Notes JSON, enabling Markdown, PDF, Anki CSV, and JSON exports without re-running earlier pipeline stages.
